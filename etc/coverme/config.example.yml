---
defaults:
    tmpdir: /tmp

backups:
  - type: database
    url: postgres://postgres:postgres@127.0.0.1:5432/postgres
    to: [bucket1]
    name: postgres-{mm}-{dd}.sql
    tags: postgres

vaults:
  bucket1:
    service: s3
    region: eu-central-1
    name: example-bucket-egtxvg
...
